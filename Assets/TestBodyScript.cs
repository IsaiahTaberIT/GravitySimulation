using UnityEngine;

public class TestBodyScript : MonoBehaviour
{
    Vector2 velocity;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
   
    void Step()
    {
     
    }



    // Update is called once per frame
    void Update()
    {
        
    }
}
